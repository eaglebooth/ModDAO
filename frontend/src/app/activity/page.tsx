import{ContractBar}from"@/components/ContractBar";import{PageHead}from"@/components/PageHead";export default function P(){return <><PageHead kicker="Verification surface" title="PROVE THE APP CALLS THE CONTRACT." copy="After V2 deployment this page will expose the configured address, Studionet sync, and Explorer links for successful lifecycle calls."/><ContractBar/><section className="band"><div className="wrap notice"><strong>Deployment pending.</strong> Add the new address only after ModDAO V2 is deployed and each write function is tested in Studio.</div></section></>}
