import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="review" id={id} kicker="Function 06 / evaluate" title="RUN THE COMPARATIVE JURY." copy="Validators compare decision, score band, and serious policy failures while ignoring harmless wording differences." steps={["Verify the log URL resolves.","Confirm the evaluation is PENDING.","Trigger review and wait for final consensus."]}/>}
