import{PageHead}from"@/components/PageHead";import{Reveal}from"@/components/Reveal";const rows=[["Helpfulness / 30","Respect, patience, useful guidance."],["Accuracy / 50","Correct resolution without harmful invention."],["Compliance / 20","Consistent rules without abuse."],["90-100","EXCELLENT payout tier."],["75-89","STANDARD payout tier."],["Below 75","NO_PAYOUT."],["Weak evidence","NEEDS_EVIDENCE; no payout proposal."]];export default function How(){return <><PageHead kicker="Full product guide" title="READ THE RUBRIC BEFORE THE LOGS." copy="ModDAO V2 exposes the criteria, state machine, appeal, and finalization rules so moderators and reviewers know exactly what happens on-chain."/><section className="band"><div className="wrap action-layout"><Reveal><h2 className="display" style={{fontSize:38}}>SCORING MAP</h2><p className="lede">Validators may phrase comments differently, but comparative consensus requires agreement on the payout decision, score band, and serious policy failures.</p></Reveal><div className="list">{rows.map(([a,b])=><Reveal className="scoring-row" key={a}><strong className="mono">{a}</strong><span>{b}</span></Reveal>)}</div></div></section><section className="band blue"><div className="wrap"><h2 className="display section-title">PENDING → RULING → APPEAL → FINALIZED</h2><p style={{maxWidth:700,lineHeight:1.8,marginTop:24}}>Evaluation never spends treasury immediately. The first review proposes a tier. The moderator may submit one appeal. Only finalization deducts the payout ledger, and double-finalization is blocked.</p></div></section></>}
