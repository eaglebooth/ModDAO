import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="deactivate" id={id} kicker="Function 04 / deactivate_moderator" title="CLOSE FUTURE REVIEW CYCLES." copy="Admin deactivation blocks new evaluations without rewriting historical audit records." steps={[`Confirm moderator #${id} is the intended record.`,"Connect the admin wallet.","Deactivate once; historical evaluations remain readable."]}/>}
