import{WorkflowPage}from"@/components/WorkflowPage";export default function P(){return <WorkflowPage mode="register" kicker="Function 03 / register_moderator" title="DECLARE PAY BEFORE PERFORMANCE." copy="The admin registers a wallet and two fixed payout tiers. The AI later selects a tier; it never invents an amount." steps={["Connect the admin wallet.","Verify the moderator wallet is not already registered.","Set standard and excellent tiers, then sign."]}/>}
