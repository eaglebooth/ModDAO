import Link from"next/link";export function ContractBar(){const a=process.env.NEXT_PUBLIC_CONTRACT_ADDRESS||"";return <div className="contract-bar"><div className="wrap contract-inner"><div><span className="dot"/><strong>{a?"ModDAO V2 connected":"Awaiting ModDAO V2 redeployment"}</strong><div className="meta mono" style={{marginTop:5}}>{a||"Writes stay disabled until the new Studio address is added."}</div></div><Link href="/activity" className="secondary">VERIFY CONNECTION</Link></div></div>}
