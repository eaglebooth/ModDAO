import Link from"next/link";export function ContractBar(){const a=process.env.NEXT_PUBLIC_CONTRACT_ADDRESS||"";return <div className="contract-bar"><div className="wrap contract-inner"><div><span className="dot"/><strong>{a?"ModDAO V2 connected":"Contract address not configured"}</strong><div className="meta mono" style={{marginTop:5}}>{a||"Add NEXT_PUBLIC_CONTRACT_ADDRESS before using contract actions."}</div></div><Link href="/activity" className="secondary">VERIFY CONNECTION</Link></div></div>}
