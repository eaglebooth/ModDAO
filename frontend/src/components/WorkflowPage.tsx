import{PageHead}from"./PageHead";import{ContractBar}from"./ContractBar";import{Workflow}from"./Workflow";type Mode="setup"|"fund"|"register"|"deactivate"|"request"|"review"|"appeal"|"appeal-review"|"finalize";export function WorkflowPage({mode,kicker,title,copy,steps,id}:{mode:Mode;kicker:string;title:string;copy:string;steps:string[];id?:string}){return <><PageHead kicker={kicker} title={title} copy={copy}/><ContractBar/><section className="band"><div className="wrap action-layout"><aside className="instructions"><span className="kicker">Preflight</span><ol>{steps.map((s,i)=><li key={s}><b>{String(i+1).padStart(2,"0")}</b><span>{s}</span></li>)}</ol></aside><Workflow mode={mode} id={id}/></div></section></>}
